def twoSum(nums, target):
    answer = []
    """
    :type nums: List[int]
    :type target: int
    :rtype: List[int]
    :type answer: List[int]
    """
    for i in range(0, len(nums)):
        for j in range(0, len(nums)):
            print(nums[i])


twoSum([2, 7, 11, 15], 9)
